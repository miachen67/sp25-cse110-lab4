1. values added: 20
2. final result: 20
3. `var` is outdated and is more commonly used in older scripts. `var` does not follow block scope and only follows global or function scope so it is not something we should use as it may lead to scoping/naming issues.
4. values added: 20
5. The code returns an error because result is not defined in this code block. We used the keyword `let` to define result so it is only defined within the `if` block. 
6. There is an error because we have tried to reassign result to a new value, which is not allowed for `const` variables.
7. There is an error  due to the same issue from question 6. 